# Ascend / Emerge Deployment Comparison

| Area | Emerge Globally | Ascend | Difference | Required Action |
| --- | --- | --- | --- | --- |
| Repository | `emergeinternational/emergy-shop-forge` | `emergeinternational/gold-agency-foundations` | Separate codebases and production brands. | Keep deployments isolated; do not connect Ascend to the Emerge Vercel project. |
| Production domain | `www.emergeglobally.com` | `www.ascendeliteagency.com` | Emerge already resolves to Vercel; Ascend still resolves to the prior host. | Change DNS for `www` to Vercel's recommended CNAME. |
| Hosting provider | Vercel, verified by `server: Vercel` and Vercel DNS. | New Vercel project created and deployed; current custom domain DNS still points away from Vercel. | Ascend's live custom domain is not yet serving the Vercel deployment. | Set `CNAME www -> 7a7eba8e32c9a538.vercel-dns-017.com.` and rerun `vercel domains verify www.ascendeliteagency.com`. |
| Vercel project | `emergy-shop-forge`, project id `prj_32NVmpVYy4H22ghcGFz92wNsIHFh`. | `ascend-elite-agency`, project id `prj_vqPb1szFGAV9qhk3CboA1W7yqLbW`. | Ascend now has its own Vercel project. | Keep `.vercel` local and uncommitted. |
| Deployment id | Known Emerge reference: `dpl_DyU9bRUtttPWBubypr7xEGHrW391`. | Ascend Vercel production deployment: `dpl_7ZFQyR6baroU7NC4fetKAKDbMKaP`. | Ascend has a production deployment URL but not final DNS cutover. | Promote future builds through the Ascend Vercel project. |
| Build command | Vite production build on Vercel. | `npm run build` on Vercel. | GitHub Pages used `build:pages`, which changes the base path and is wrong for Vercel/custom domain. | Use `npm run build`; do not use `build:pages` for production Vercel. |
| SPA routing | `vercel.json` rewrites non-API routes to `/index.html`. | Added `vercel.json` with the same SPA fallback pattern. | Ascend did not have Vercel SPA fallback config before this work. | Keep the Vercel rewrite so `/submit`, `/admin`, and refreshes work. |
| GitHub Actions | No Pages workflow is used for Emerge production. | Pages workflow existed and failed at repository permission/configuration. | The Pages path was an unproven deployment mechanism for Ascend. | Remove the Pages workflow; use Vercel for production deployment. |
| Environment variables | Supabase env vars configured in the Vercel project. | Vercel has encrypted `VITE_SUPABASE_PROJECT_ID`, `VITE_SUPABASE_URL`, and `VITE_SUPABASE_PUBLISHABLE_KEY` for production, preview, and development. | Ascend envs are now configured independently. | Do not commit `.env`, `.env.local`, or `.vercel`. |
| Supabase project | `fqphlzqactfvaiuoahcd`. | `fqphlzqactfvaiuoahcd`. | Same Supabase project is used by the current Ascend app. | Keep anon-only browser access; use private media storage policies. |
| Public submission proof | Emerge casting flow is the reference pattern. | Vercel UI submission reached `/submission-success?id=1a3c447d-bf9e-46a6-87c7-ac66192286b0`. | Ascend public flow is verified on the Vercel deployment URL, not yet the custom domain. | Repeat the same live test after DNS cutover. |
| Storage behavior | Browser uploads to existing buckets only. | Browser code uploads to `ascend-applicant-media`; no browser `createBucket` call found. | Ascend now follows the secure provision-by-migration pattern. | Keep bucket provisioning in migrations/admin infrastructure only. |
| Admin verification | Emerge admin was previously verified separately. | Vercel `/admin/review` redirects to sign-in in this browser session. | Admin UI record/media verification requires an authenticated Ascend admin session or Supabase access token. | Sign in on the Vercel/custom domain or provide Supabase CLI auth to verify private row/media details directly. |

